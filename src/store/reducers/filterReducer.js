// const defaultState = {
// 	filtredData: [],
// };

// const FILTER_DATA = "FILTER_DATA";
// export const filterReducer = (state = defaultState, action) => {
// 	switch (action.type) {
// 		case FILTER_CARDS:
// 			return {
// 				...state,
// 				filtredData: state?.data?.filter(
// 					(project) =>
// 						Number(hotel.result_object.rating) >= state.rating
// 				),
// 			};
// 		case RESET_FILTERS:
// 			return { ...state, filtredCards: state.data };
// 		default:
// 			return state;
// 	}
// };

